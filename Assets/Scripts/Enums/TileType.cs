using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace ReTD.Enums {
    public enum TileType {
        Buildable = 0,
        Path = 1,
        Base = 2,
        Spawner = 3,
        Turret = 4
    }
}