using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class BuildInterface : MonoBehaviour {
    public GameObject buildGUI;

    private void Start() {
        //Helpers.GetGameManager().ShowUI(buildGUI, transform);
    }
}
